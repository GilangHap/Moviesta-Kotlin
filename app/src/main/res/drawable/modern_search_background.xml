<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Sudut membulat -->
    <corners android:radius="16dp"/>
    <!-- Background dengan gradien halus -->
    <gradient
        android:startColor="@color/surface_color"
        android:endColor="@color/card_surface"
        android:angle="135" />
    <!-- Border tipis -->
    <stroke
        android:width="1dp"
        android:color="@color/text_secondary" />
</shape>